# 🌐 번역 및 다중 에이전트 기능 계획

> **목표:** Tiny MoA에 다국어 지원 및 전문화된 에이전트 시스템 구축

---

## 📋 현재 상태 및 배경

### PoC 완료 항목
- ✅ Brain (LFM2.5-1.2B) + Reasoner (Falcon-R-0.6B) 조합
- ✅ 기본 라우팅 시스템 (코딩/수학 → Reasoner, 일반 → Brain)
- ✅ llama.cpp 기반 CPU 추론

### 추가 필요 기능
- ❌ 다국어 입력 지원 (한국어, 일본어, 중국어 등)
- ❌ 한국 문화 특화 RAG 시스템
- ❌ 전문화된 에이전트 분리 (Tool별, 프롬프트별)
- ❌ 파일 참조 기능 (@ 멘션 → Docling 변환)
- ❌ 협업 에이전트 시스템 (Tiny Cowork)

---

## 🎯 구현 목표

### Phase 1: 번역 파이프라인 (Google Translate 기반)

```
사용자 입력 (다국어)
       │
       ▼
┌─────────────────────────────────────────┐
│      🔤 언어 감지 (Language Detection)   │
│  - Google Translate API 사용            │
│  - 입력 언어 코드 저장 (ko, ja, zh 등)   │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│      🔄 영어로 번역 (To English)         │
│  - 모든 입력을 영어로 통일              │
│  - 모델은 영어만 처리                   │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│      🧠 모델 처리                        │
│  - Brain (LFM2.5-1.2B)                  │
│  - Reasoner (Falcon-R-0.6B)             │
│  - Tool Caller (Falcon-90M)             │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│      🔄 원래 언어로 번역 (From English)  │
│  - 저장된 언어 코드로 다시 번역          │
│  - 사용자에게 자연스러운 응답 제공       │
└─────────────────────────────────────────┘
       │
       ▼
   최종 응답 (원래 언어)
```

### Phase 2: 한국 문화 RAG 에이전트

```python
# 구조 예시
KOREAN_CULTURE_AGENT = {
    "name": "korean_culture_expert",
    "description": "한국 문화, 역사, 전통에 대한 질문 처리",
    "data_sources": [
        "korean-history-dataset",      # 한국 역사 데이터셋
        "korean-culture-wiki",         # 문화 위키 데이터
        "korean-tradition-knowledge",  # 전통 지식 베이스
    ],
    "model": "shared_brain",  # 이미 로딩된 Brain 모델 재사용
    "rag_enabled": True
}
```

#### 데이터셋 후보
| 데이터셋 | 용도 | 출처 |
|----------|------|------|
| 한국어 위키피디아 | 일반 지식 | HuggingFace |
| KorQuAD | QA 형식 | AI Hub |
| 한국 문화유산 데이터 | 문화재 정보 | 국립중앙박물관 |
| 한국학 DB | 역사/문화 | 한국학중앙연구원 |

### Phase 3: 다중 에이전트 시스템 (Tiny Cowork)

> **영감**: Claude Cowork과 유사한 협업 AI 시스템

```
┌──────────────────────────────────────────────────────────────┐
│                    🎯 Orchestrator                           │
│          (작업 분배 및 결과 통합)                             │
└──────────────────────────────────────────────────────────────┘
              │
    ┌─────────┼─────────┬─────────┬─────────┐
    ▼         ▼         ▼         ▼         ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ Brain  │ │Reasoner│ │ Tool   │ │Culture │ │  RAG   │
│ Agent  │ │ Agent  │ │Caller  │ │ Agent  │ │ Agent  │
└────────┘ └────────┘ └────────┘ └────────┘ └────────┘
   ↓          ↓          ↓          ↓          ↓
(LFM2.5)  (Falcon-R)  (Falcon   (Shared)  (Shared)
                       90M)
```

#### 에이전트 특성
| 에이전트 | 역할 | 모델 | 전용 프롬프트 |
|----------|------|------|---------------|
| Brain Agent | 일반 대화, 라우팅 | LFM2.5-1.2B | 기본 시스템 프롬프트 |
| Reasoner Agent | 코딩/수학 | Falcon-R-0.6B | CoT 프롬프트 |
| Tool Caller | Function Calling | Falcon-90M | Tool Schema 포함 |
| Culture Agent | 한국 문화 특화 | Brain 재사용 | 문화 전문가 프롬프트 |
| RAG Agent | 문서 검색/요약 | Brain 재사용 | RAG 시스템 프롬프트 |

### Phase 4: 파일 참조 기능 (@ 멘션)

```
사용자: @report.pdf 이 문서 요약해줘

       │
       ▼
┌─────────────────────────────────────────┐
│      📄 Docling 변환                     │
│  - PDF/DOCX/PPTX → Markdown 변환        │
│  - 구조화된 텍스트 추출                  │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│      📝 컨텍스트 주입                    │
│  - 변환된 Markdown을 프롬프트에 추가     │
│  - 청킹 처리 (큰 문서 대응)              │
└─────────────────────────────────────────┘
       │
       ▼
   모델 처리 → 응답
```

---

## 🔧 Function Calling 전략

### 모델 조합
| 역할 | 모델 | 특징 |
|------|------|------|
| **JSON 생성** | Falcon-H1-Tiny-Tool-Calling-90M | 90M 파라미터, 영어 전용, 간헐적 오타 발생 |
| **보정/검증** | LFM2.5-1.2B-Instruct | 1.2B 파라미터, Tool Use 기능 내장 |

### 파이프라인
```
Tool 호출 필요
       │
       ▼
┌─────────────────────────────────────────┐
│  Falcon-90M: JSON 생성                   │
│  - 빠른 응답 (90M)                       │
│  - 오타 가능성 있음                      │
└─────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  LFM2.5: JSON 검증 및 보정               │
│  - 문법 오류 수정                        │
│  - 누락된 필드 보완                      │
│  - 최종 Tool 호출                        │
└─────────────────────────────────────────┘
```

---

## 📁 파일 구조 (확장)

```
src/tiny_moa/
├── __init__.py
├── brain.py              # 라우터 + Brain 에이전트
├── reasoner.py           # Reasoner 에이전트
├── tool_caller.py        # Tool Calling 에이전트
├── agents/               # [NEW] 전문화 에이전트
│   ├── __init__.py
│   ├── base_agent.py     # 기본 에이전트 클래스
│   ├── culture_agent.py  # 한국 문화 에이전트
│   └── rag_agent.py      # RAG 에이전트
├── translation/          # [NEW] 번역 모듈
│   ├── __init__.py
│   ├── detector.py       # 언어 감지
│   ├── translator.py     # Google Translate 래퍼
│   └── pipeline.py       # 번역 파이프라인
├── rag/                  # [NEW] RAG 시스템
│   ├── __init__.py
│   ├── embeddings.py     # 임베딩 모델
│   ├── vectorstore.py    # 벡터 저장소
│   └── retriever.py      # 검색기
├── docling/              # [NEW] 문서 변환
│   ├── __init__.py
│   └── converter.py      # Docling 래퍼
├── cowork/               # [NEW] 협업 시스템
│   ├── __init__.py
│   ├── orchestrator.py   # 작업 분배
│   └── coordinator.py    # 에이전트 조율
├── tools/                # 개별 도구
│   ├── __init__.py
│   ├── weather.py
│   ├── search.py
│   └── calculator.py
├── orchestrator.py       # 메인 오케스트레이터
└── main.py
```

---

## 📦 의존성 추가

```txt
# requirements.txt (추가)

# 번역
googletrans==4.0.0-rc1    # Google Translate (무료)
# 또는
google-cloud-translate    # Google Cloud Translation (유료, 정확도 높음)
langdetect>=1.0.9         # 언어 감지 (백업)

# RAG
sentence-transformers>=2.2.0  # 임베딩
chromadb>=0.4.0              # 벡터 DB
faiss-cpu>=1.7.4             # 대안 벡터 DB

# 문서 변환
docling>=0.1.0               # PDF/DOCX → Markdown

# 기타
requests>=2.31.0             # API 호출
pydantic>=2.0.0              # 데이터 검증
```

---

## ⏱️ 예상 일정

| 단계 | 작업 | 예상 시간 |
|------|------|-----------|
| 1 | 번역 파이프라인 (Language Detection + Translate) | 3시간 |
| 2 | 한국 문화 RAG 데이터셋 수집/정리 | 4시간 |
| 3 | RAG 시스템 구축 (Embedding + VectorDB) | 4시간 |
| 4 | Docling 파일 변환 기능 | 2시간 |
| 5 | 전문 에이전트 클래스 설계 | 2시간 |
| 6 | Tiny Cowork 오케스트레이터 | 3시간 |
| 7 | Function Calling (Falcon-90M + LFM2.5 검증) | 3시간 |
| 8 | 통합 테스트 및 디버깅 | 3시간 |
| **총계** | | **~24시간** |

---

## 🆚 우선순위 비교: Tool Calling vs 번역/다중 에이전트

### tool_calling_plan.md (기존)
- **예상 시간**: ~12시간
- **핵심 기능**: 날씨/검색/계산기 API 호출
- **즉시 효과**: 실시간 정보 접근 가능

### translation_multiagent_plan.md (신규)
- **예상 시간**: ~24시간
- **핵심 기능**: 다국어 지원, 한국 문화 RAG, 협업 시스템
- **즉시 효과**: 한국어 사용자 경험 개선

### 🎯 권장 순서

```
1️⃣ Tool Calling (12시간)
   └─ 이유: 기반 인프라 (Tool Schema, Executor)가 다중 에이전트에도 필요

2️⃣ 번역 파이프라인 (3시간)
   └─ 이유: 가장 빠르게 한국어 지원 가능

3️⃣ Function Calling 개선 (3시간)
   └─ 이유: Falcon-90M + LFM2.5 검증 파이프라인

4️⃣ 한국 문화 RAG (8시간)
   └─ 이유: 데이터셋 수집 필요, 시간 소요

5️⃣ Tiny Cowork (5시간)
   └─ 이유: 모든 에이전트 완성 후 통합
```

---

## ⚠️ 주의사항

1. **Google Translate 제한**: 무료 API는 일일 할당량 존재
2. **Falcon-90M 영어 전용**: 한국어 직접 처리 불가
3. **RAG 데이터 품질**: 한국 문화 데이터셋 품질 검증 필요
4. **메모리 관리**: 여러 에이전트 동시 로딩 시 메모리 주의

---

## 📚 참고 자료

- [Falcon-H1-Tiny-Tool-Calling-90M](https://huggingface.co/tiiuae/Falcon-H1-Tiny-Tool-Calling-90M)
- [Falcon-H1-Tiny-Tool-Calling-90M-GGUF](https://huggingface.co/tiiuae/Falcon-H1-Tiny-Tool-Calling-90M-GGUF)
- [LFM2.5-1.2B-Instruct Tool Use](https://huggingface.co/LiquidAI/LFM2.5-1.2B-Instruct#tool-use)
- [Docling](https://github.com/DS4SD/docling)
- [ChromaDB](https://www.trychroma.com/)
- [Sentence Transformers](https://www.sbert.net/)

---

## 🔮 향후 확장

### Tiny Cowork 고급 기능
- **협업 프로토콜**: 에이전트 간 메시지 전달
- **공유 메모리**: 세션 컨텍스트 유지
- **태스크 분해**: 복잡한 작업 자동 분할
- **병렬 처리**: 독립 태스크 동시 실행

### 다국어 확장
- 일본어, 중국어 문화 RAG 추가
- 각 언어별 전문 에이전트 확장
